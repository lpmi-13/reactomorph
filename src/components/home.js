import React from 'react';

const Home = () => {
  return (
    <div>
      <p>
        Sometimes, it's nice to know that a word like "unite" is related to other words like "unity", "union", and
        "unionize".
      </p>
      <p>On the "Search" page, you can search for words like this and see the related words.</p>
    </div>
  );
};

export default Home;
